fastapi==0.111.0
httpx==0.27.0
numpy>=1.26.4,<2.0.0
pandas>=2.0.0
yfinance==0.2.40
aiosqlite==0.20.0
python-multipart>=0.0.9

# Phase 0 — Postgres, config, logging (needed by backtest + refresh-ohlcv endpoints)
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
pydantic-settings>=2.3.0
python-json-logger>=2.0.7

# Phase 2 — ML packages are NOT included here (Vercel bundle).
# sklearn + scipy + xgboost = ~287MB unzipped, exceeding Vercel's 250MB limit.
# These are installed via backend/requirements.txt for local dev and training only.
# On Vercel, _ml_shadow() catches the ImportError and returns {status: "no_ml"}.
# Future: convert models to ONNX (onnxruntime ~15MB) to enable Vercel ML inference.
