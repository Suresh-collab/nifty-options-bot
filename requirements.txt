fastapi==0.111.0
httpx==0.27.0
numpy>=1.26.4,<2.0.0
pandas>=2.0.0
yfinance==0.2.40
aiosqlite==0.20.0
python-multipart>=0.0.9

# Phase 0 — Postgres, config, logging (needed by backtest + refresh-ohlcv endpoints)
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
pydantic-settings>=2.3.0
python-json-logger>=2.0.7

# Phase 3 — ONNX inference (~15MB, fits Vercel's 50MB limit).
# sklearn + xgboost (~287MB) stay in backend/requirements.txt for local training only.
# After running backend/scripts/export_onnx.py, the ML shadow panel works on Vercel.
onnxruntime>=1.17.0
